version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - ./logs:/app/logs
      - h2-data:/data
    restart: unless-stopped

volumes:
  h2-data:
    driver: local 